# Include all source subdirectories

# Add board library
add_subdirectory(board)

# Add engine library
add_subdirectory(engine)

# Add main executable
add_subdirectory(main)

# Add common utilities (placeholder for future expansion)
add_subdirectory(common)